import requests
from bs4 import BeautifulSoup
from dotenv import load_dotenv
import pandas as pd

load_dotenv()

print("환경 설정이 정상적으로 완료되었습니다!")
print("requests, beautifulsoup4, python-dotenv, pandas 모두 정상 작동 중입니다.")